---
const baseDir = import.meta.env.BASE_URL;
/*
 * トップページ専用のローディングオーバーレイ
 * - ファーストビューで見せたい画像には data-critical 属性を付与
 * - 下層ページには配置しない（チラつき防止はCSSで全ページ対応）
 */
---

<!-- 
  ローディングオーバーレイ
  ============================================================
  初期状態: opacity: 0, display: flex（非表示だが配置はされている）
  
  【動作】
  1. 読み込みが250ms以上かかる場合のみ opacity: 1 で表示
  2. 高速読み込み時は非表示のまま display: none に変更されて終了
  3. 表示した場合は最低1000ms表示した後フェードアウト
  ============================================================
-->
<div id="js-loading-overlay" class="loading-overlay">
  <div class="loading-content">
    <!-- ロゴアニメーション -->
    <div class="logo-container">
      <img src={`${baseDir}assets/img/logo/logo.svg`} alt="Logo" width="120" height="40" />
    </div>

    <!-- プログレスバー -->
    <div class="progress-container">
      <div id="js-progress-bar" class="progress-bar"></div>
    </div>
  </div>
</div>

<style>
  /* ============================================================
   * ページ読み込み中のチラつき防止（全ページ共通）
   * ============================================================ */

  /* 読み込み中: bodyを非表示 */
  :global(html.page-loading) {
    overflow: hidden; /* スクロール防止 */
  }

  :global(html.page-loading body) {
    opacity: 0;
    /* transition は page-loaded 側で定義（片方向のみ） */
  }

  /* 読み込み完了: bodyをフェードイン */
  :global(html.page-loaded body) {
    opacity: 1;
    transition: opacity 0.3s ease-out;
  }

  /* ============================================================
   * ローディングオーバーレイのスタイル
   * ============================================================ */

  .loading-overlay {
    display: flex; /* 初期状態で配置 */
    position: fixed;
    justify-content: center;
    align-items: center;

    /* 初期状態は透明（JSで制御） */
    opacity: 0;
    z-index: 9999;

    /* フェードイン・アウトのアニメーション */
    transition: opacity 0.4s cubic-bezier(0.4, 0, 0.2, 1);
    inset: 0;
    background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
  }

  /* ============================================================
   * ローディングコンテンツ
   * ============================================================ */

  .loading-content {
    text-align: center;
  }

  /* ロゴの浮遊アニメーション */
  .logo-container {
    animation: float 2s ease-in-out infinite;
    margin-bottom: 2rem;
  }

  @keyframes float {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(-10px);
    }
  }

  /* ============================================================
   * プログレスバー
   * ============================================================ */

  .progress-container {
    border-radius: 2px;
    background: rgba(255, 255, 255, 0.2);
    width: 200px;
    height: 2px;
    overflow: hidden;
  }

  .progress-bar {
    transform: scaleX(0);

    /* 左端を起点にスケール */
    transform-origin: left;

    /* スムーズなアニメーション */
    transition: transform 0.3s cubic-bezier(0.4, 0, 0.2, 1);
    border-radius: 2px;
    background: white;
    height: 100%;
  }
</style>
